// import * as cache from '@actions/cache';


export async function LoadCache() {
    // const repo = process.env.GITHUB_REPOSITORY;
    // const operationSystem = process.platform;
    // const architecture = process.arch;
    // const sourceBranch = process.env.GITHUB_REF_NAME;
    // const targetBranch = process.env.GITHUB_BASE_REF;
    // // take the cache from this branch if exists, otherwise take the cache from the target branch (e.g main)
    // const cacheKey = `${repo}:${operationSystem}:${architecture}:${sourceBranch}`;
    // const fallbackCacheKey = `${repo}:${operationSystem}:${architecture}:${targetBranch}`;
    // cache.restoreCache([], cacheKey, fallbackCacheKey);

}